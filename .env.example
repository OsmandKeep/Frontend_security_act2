# Backend upstream API URL (host.docker.internal allows Docker containers to reach host machine services)
API_UPSTREAM=http://host.docker.internal:8000

# Secret API Key injected securely on the server side by Nginx
API_KEY=my-secret-key
